<link rel="stylesheet" href="../css/navbar.css">

  <nav id="nav-bar" class="overlay">

    <a href="#" class="icon"><img src="../images/AnimatedLogo.gif" alt="Text-Tools"></a>

    <div class="overlay-content">
      <a href="/#services"><i class="fa-solid fa-laptop"></i>Services</a>
      <a href="/#about"><i class="fa-solid fa-book"></i>About</a>
      <a href="/userpage"><i class="fa-solid fa-user"></i>User</a>
      <a href="/logout" class="btn btn-outline-danger"><i class="fa-solid fa-right-to-bracket"></i>Logout</a>
    </div>

  </nav>

  <div class="nav-mob"> <button id="nav-btn" onclick="openNav()">☰</button> </div>


<script>

   
 const openNav = ()=>{
  if(screen.width <= '450'){
  document.querySelector('.overlay-content').style.opacity = '100%';
  document.querySelector('.nav-mob').style.opacity = '0%';
  document.querySelector("#nav-bar").style.width = '100%';
  document.querySelector('body').style.overflowY = 'hidden';
  }
}

const closeNav = ()=>{
  if(screen.width <= '450'){
  document.querySelector('body').style.overflowY = 'visible';
  document.querySelector('.overlay-content').style.opacity = '0%';
  document.querySelector('.nav-mob').style.opacity = '70%';
  document.querySelector('#nav-bar').style.width = '0%';
}
 }

  const links = document.querySelectorAll('.overlay-content a');
  for(let i = 0 ; i < links.length ; i++){
    links[i].addEventListener('click',()=>{
      closeNav();
    })
  }

  document.addEventListener('mouseup',(e)=>{
    let space = document.querySelector('.overlay-content');
    if (!space.contains(e.target) && screen.width <= '450') {
        closeNav();
    }
  })


  
var prevScrollpos = window.pageYOffset;

window.addEventListener('scroll',()=>{
  console.log('triggered')
}) 

  // if(screen.width > '450'){
  //   var currentScrollPos = window.pageYOffset;
  // if (prevScrollpos > currentScrollPos) {
  //   document.getElementById("nav-bar").style.top = "0";
  // } else {
  //   document.getElementById("nav-bar").style.top = "-5rem";
  // }
  // prevScrollpos = currentScrollPos;
  // }
  



</script>
    
 